# Two-session approval test — expense report full chain
# Version: 1.4
# Requires: VS, AK
# Notes: B-UI-3.4 migration (CMD70).
#        Click ForInstance replaces bare Click on queue rows.
#        Wait ForQueueRow replaces the two dormant UI-render-gap Pauses
#        from v1.2 (lines 44 and 53 — commented out when B-UI-1 shipped,
#        now activated as real Waits).
#
#        Three Pauses retained pending future briefs:
#        (a) Form-render: awaits Cadence iframe compass_form_ready
#            → TODO: Wait ForForm
#        (b) Review popup modal: awaits modal.opened emit
#            → TODO: Wait ForModal
#        (c) Document Review panel: awaits modal.opened emit
#            → TODO: Wait ForModal
#
#        Candidate cleanup (not in v1.4 scope):
#        - Lines 19 and 23 `Wait 500` hand-waits — predate B2 typed Waits
#        - AK's Set Tab "MY TIME" / "MY WORK" tab-twiddle at step 2 —
#          may no longer be needed with reactive queues + CMD70

# — preflight · presence ————————————————————————————————
Assert session VS is connected
Assert session AK is connected

# — preflight · navigate both sessions to Compass ————————
VS: Set View "compass"
AK: Set View "compass"
Wait ForLocation VS "compass.my_work" timeout=30000
Wait ForLocation AK "compass.my_work" timeout=30000
VS: Set Tab "MY WORK"
AK: Set Tab "MY WORK"

# — VS submits ————————————————————————————————————————
VS: Set Tab "MY REQUESTS"
VS: Set SubTab "BROWSE"
VS: Form Open "Expense Report"
Pause Form open — confirm Expense Report modal is visible on Vaughn's screen
# TODO: replace with Wait ForForm after Cadence iframe compass_form_ready migration
VS: Form Insert "Employee Name" "Vaughn Staples"
VS: Form Insert "Trip Start Date" "2026-04-14"
VS: Form Insert "Trip End Date" "2026-04-14"
VS: Form Select "Business Purpose" "client"
Wait 500
VS: Form Insert "Purpose Description" "Two-session CMD Center approval test"
VS: Form Insert "Customer Name" "Apex Consulting Group"
VS: Form Insert "Customer Location" "Phoenix, AZ"
Wait 500
VS: Form Submit
Wait ForEvent "form.submitted" → $instance_id
Wait ForInstance $instance_id for launched
Log "Package live · $instance_id"

# — step 1: VS reviews ————————————————————————————————
VS: Set Tab "MY WORK"
Wait ForRoute $instance_id to VS
Wait ForQueueRow $instance_id to VS
VS: Click ForInstance $instance_id "Review"
Pause Confirm Review popup is raised on Vaughn's screen
# TODO: replace with Wait ForModal via modal.opened emit
VS: Click "Approve"
Wait ForRoute $instance_id to AK
Log "Step 1 complete"

# — step 2: AK approves ———————————————————————————————
AK: Set Tab "MY TIME"
AK: Set Tab "MY WORK"
Wait ForQueueRow $instance_id to AK
AK: Click ForInstance $instance_id "Approve"
Pause Confirm Document Review panel is visible on Angela's screen
# TODO: replace with Wait ForModal via modal.opened emit
AK: Click "Approve"
Wait ForInstance $instance_id for completed timeout=30000
Log "Step 2 complete · AK approved"

Log "✓ Chain confirmed · steps 1-3 complete · $instance_id"
# Version: 1.1
# Requires: VS
# B1 emit probe — submits a tiny form and listens for all seven events.
# Will time out on events that haven't been wired yet; that is diagnostic.
# v1.1: B2 migration — Compass-load Pause replaced with Wait ForLocation.
#       Form Insert/Select syntax corrected to quoted human labels.

Assert session VS is connected

VS: Set View "compass"
Wait ForLocation VS "compass.my_work" timeout=30000

# Probe emit #1 verified by the Wait ForLocation above.

# Probe emits #2, #3, #5
VS: Set Tab "MY REQUESTS"
VS: Set SubTab "BROWSE"
VS: Form Open "Expense Report"
Pause Form open — confirm Expense Report modal is visible
# TODO: replace with Wait ForForm after Cadence iframe migration
VS: Form Insert "Employee Name" "Vaughn Staples"
VS: Form Insert "Trip Start Date" "2026-04-14"
VS: Form Insert "Trip End Date" "2026-04-14"
VS: Form Select "Business Purpose" "client"
VS: Form Insert "Purpose Description" "B1 event probe"
VS: Form Insert "Customer Name" "Apex Consulting Group"
VS: Form Submit

Wait ForEvent "form.submitted" timeout=10000 → $instance_id
Wait ForInstance $instance_id for launched timeout=5000
Wait ForRoute $instance_id to VS timeout=10000

Log "✓ B1 emits 1, 2, 3, 5 verified for instance $instance_id"